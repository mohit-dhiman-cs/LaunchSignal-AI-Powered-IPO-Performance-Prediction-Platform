# ── LaunchSignal Frontend Configuration ────────────────────────────────

# The base URL of the active Flask backend API server
# Defaults to http://localhost:5000 in development
VITE_API_URL=http://localhost:5000
